_schema-version: "3.2"
ID: uiruleengine
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: uiruleengine-destination-content
  type: com.sap.application.content
  requires:
  - name: uiruleengine-destination-service
    parameters:
      content-target: true
  - name: uiruleengine-repo-host
    parameters:
      service-key:
        name: uiruleengine-repo-host-key
  - name: uiruleengine-uaa
    parameters:
      service-key:
        name: uiruleengine-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: uiruleengine_html_repo_host
          ServiceInstanceName: uiruleengine-html5-service
          ServiceKeyName: uiruleengine-repo-host-key
          sap.cloud.service: uiruleengine
        - Authentication: OAuth2UserTokenExchange
          Name: uiruleengine_uaa
          ServiceInstanceName: uiruleengine-xsuaa-service
          ServiceKeyName: uiruleengine-uaa-key
          sap.cloud.service: uiruleengine
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: uiruleengine-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: uiruleengine-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - uiruleengine.zip
      name: uiruleengine
      target-path: resources/
- name: uiruleengine
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: uiruleengine-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: uiruleengine-destination-service
    service-plan: lite
- name: uiruleengine-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: uiruleengine-xsuaa-service
    service-plan: application
- name: uiruleengine-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: uiruleengine-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
